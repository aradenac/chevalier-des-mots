{
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "docs:serve": "mkdocs serve",
    "docs:build": "mkdocs build",
    "trace": "bash tools/trace.sh",
    "verify": "npm test && npm run build && npm run trace"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
